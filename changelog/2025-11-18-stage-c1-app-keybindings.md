# 2025-11-18 阶段C-1：整理 App 快捷键映射\n\n## 变更\n- 重构 `src/App.tsx` 中的键盘事件处理：\n  - 将原本 `useEffect` 内一长串 `if`/`return` 条件判断，整理为 `KEY_BINDINGS` 常量数组。\n  - 每条绑定包含 `match` / `preventDefault` / `action`，对应：\n    - Ctrl+Z 撤销；\n    - `+`/`=` 与 `-` 缩放；\n    - G 切换网格；B/M 切换工具；\n    - 数字键 0-9 切换调色板颜色；\n    - 方向键 / WASD 平移视图；\n    - F 填充选区，Esc 取消选区。\n- 保持所有快捷键行为与之前完全一致，仅提升结构清晰度，便于后续补文档与扩展。\n\n## 测试\n- 暂未执行（建议在本地运行：`npm run lint && npm run test && npm run test:bench`）。\n
